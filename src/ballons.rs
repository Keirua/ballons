#[derive(Debug, PartialEq, Eq, Hash, Copy, Clone)]
pub enum Balloons {
	Red,
	Blue,
	Green,
	Violet,
	Yellow
}

#[derive(Debug, PartialEq, Eq, Hash, Copy, Clone)]
pub enum ActionCard {
	Red,
	Blue,
	Green,
	Violet,
	Yellow,
	Parent
}